micronaut:
  router:
    static-resources:
      swagger:
        paths: classpath:META-INF/swagger
        mapping: /swagger/**
  session:
    http:
      cookie: true
      header: true
  application:
    name: StarWarsMicroService
  server:
    cors:
      enabled: true
    port: 3001
